services:
  web:
    build: .
    command: /app/entrypoint.sh
    volumes:
      - .:/app
      - media_volume:/app/media
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
    stdin_open: true
    tty: true

volumes:
  media_volume:
